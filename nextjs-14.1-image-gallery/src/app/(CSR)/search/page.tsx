import SearchPage from "./SearchPage"

export const metadata = { // can only be exported from server components
    title: "Search - NextJS 14.1 Image Gallery"
}


export default function Page(){
    return (
        <SearchPage/>
    )
}